diff --git a/src/styles/community.css b/src/styles/community.css
index 582e245..3ccdc45 100644
--- a/src/styles/community.css
+++ b/src/styles/community.css
@@ -45,8 +45,8 @@
   border-radius: var(--radius-xl);
   padding: var(--spacing-xl);
   box-shadow: var(--glass-shadow);
-  
-  -webkit-
+  backdrop-filter: blur(16px);
+  -webkit-backdrop-filter: blur(16px);
   margin-bottom: var(--spacing-xl);
   transition: transform var(--transition-normal);
 }
diff --git a/src/styles/design-system.css b/src/styles/design-system.css
index f31b671..87cc3bc 100644
--- a/src/styles/design-system.css
+++ b/src/styles/design-system.css
@@ -196,8 +196,8 @@
    ========================================================================== */
 .glass-panel {
   background: var(--glass-bg-base);
-  
-  -webkit-
+  backdrop-filter: blur(16px);
+  -webkit-backdrop-filter: blur(16px);
   border: 1px solid var(--glass-border);
   border-radius: var(--radius-xl);
   padding: 1.5rem;
@@ -206,21 +206,21 @@
 
 .glass-card {
   background: linear-gradient(135deg, rgba(255, 255, 255, 0.07) 0%, rgba(255, 255, 255, 0.02) 100%);
-  
-  -webkit-
+  backdrop-filter: blur(16px);
+  -webkit-backdrop-filter: blur(16px);
   border: 1px solid var(--glass-border);
   border-radius: var(--radius-xl);
   padding: 1.5rem;
   box-shadow: var(--shadow-glass);
-  transition: all var(--transition-normal);
+  transition: background-color var(--transition-normal), border-color var(--transition-normal), box-shadow var(--transition-normal), transform var(--transition-normal), opacity var(--transition-normal), color var(--transition-normal);
   display: flex;
   flex-direction: column;
 }
 
 .glass-nav {
   background: rgba(6, 35, 39, 0.7);
-  
-  -webkit-
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
   border-bottom: 1px solid var(--glass-border);
   box-shadow: var(--shadow-sm);
 }
diff --git a/src/styles/globals.css b/src/styles/globals.css
index cc50f9c..51bbcbb 100644
--- a/src/styles/globals.css
+++ b/src/styles/globals.css
@@ -152,7 +152,7 @@ img, video {
   border: none;
   border-radius: var(--radius-full);
   cursor: pointer;
-  transition: all var(--transition-normal);
+  transition: background-color var(--transition-normal), border-color var(--transition-normal), box-shadow var(--transition-normal), transform var(--transition-normal), opacity var(--transition-normal), color var(--transition-normal);
   user-select: none;
   position: relative;
   overflow: hidden;
@@ -276,7 +276,7 @@ img, video {
   cursor: pointer;
   padding: 0.25rem 0.5rem;
   border-radius: 6px;
-  transition: all 0.2s ease;
+  transition: background-color 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease, opacity 0.2s ease, color 0.2s ease;
 }
 
 .card-link:hover, .card-action:hover {
@@ -448,7 +448,7 @@ img, video {
   border: 2px dashed rgba(89, 182, 194, 0.4);
   border-radius: 20px;
   background: rgba(6, 35, 39, 0.5);
-  transition: all 0.3s ease;
+  transition: background-color 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease, opacity 0.3s ease, color 0.3s ease;
 }
 
 .upload-dropzone:hover {
@@ -512,7 +512,7 @@ img, video {
   border-radius: 16px;
   background: rgba(6, 35, 39, 0.65);
   border: 1px solid rgba(255, 255, 255, 0.1);
-  transition: all 0.25s ease;
+  transition: background-color 0.25s ease, border-color 0.25s ease, box-shadow 0.25s ease, transform 0.25s ease, opacity 0.25s ease, color 0.25s ease;
 }
 
 .report-card:hover {
@@ -617,7 +617,7 @@ img, video {
   align-items: center;
   gap: 0.5rem;
   box-shadow: 0 4px 12px rgba(21, 109, 120, 0.4);
-  transition: all 0.2s ease;
+  transition: background-color 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease, opacity 0.2s ease, color 0.2s ease;
 }
 
 .btn-primary:hover {
@@ -637,7 +637,7 @@ img, video {
   display: inline-flex;
   align-items: center;
   gap: 0.5rem;
-  transition: all 0.2s ease;
+  transition: background-color 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease, opacity 0.2s ease, color 0.2s ease;
 }
 
 .btn-secondary:hover {
@@ -656,7 +656,7 @@ img, video {
   display: inline-flex;
   align-items: center;
   gap: 0.4rem;
-  transition: all 0.2s ease;
+  transition: background-color 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease, opacity 0.2s ease, color 0.2s ease;
 }
 
 .btn-outline:hover {
@@ -698,7 +698,7 @@ label {
   color: var(--color-white);
   font-family: inherit;
   font-size: var(--text-body);
-  transition: all var(--transition-fast);
+  transition: background-color var(--transition-fast), border-color var(--transition-fast), box-shadow var(--transition-fast), transform var(--transition-fast), opacity var(--transition-fast), color var(--transition-fast);
 }
 
 .textarea {
@@ -772,8 +772,8 @@ label {
   position: fixed;
   inset: 0;
   background: rgba(4, 20, 22, 0.8);
-  
-  -webkit-
+  backdrop-filter: blur(8px);
+  -webkit-backdrop-filter: blur(8px);
   z-index: var(--z-modal-backdrop);
   display: flex;
   align-items: center;
@@ -841,7 +841,7 @@ label {
   font-weight: var(--weight-medium);
   border-radius: var(--radius-full);
   cursor: pointer;
-  transition: all var(--transition-fast);
+  transition: background-color var(--transition-fast), border-color var(--transition-fast), box-shadow var(--transition-fast), transform var(--transition-fast), opacity var(--transition-fast), color var(--transition-fast);
   white-space: nowrap;
   color: var(--color-gray-300);
 }
@@ -904,7 +904,7 @@ label {
   box-shadow: 0 10px 25px rgba(239, 68, 68, 0.6);
   z-index: var(--z-sos);
   cursor: pointer;
-  transition: all var(--transition-fast);
+  transition: background-color var(--transition-fast), border-color var(--transition-fast), box-shadow var(--transition-fast), transform var(--transition-fast), opacity var(--transition-fast), color var(--transition-fast);
   animation: pulse 2s infinite;
 }
 
@@ -935,7 +935,7 @@ label {
   font-size: var(--text-label);
   font-weight: var(--weight-medium);
   cursor: pointer;
-  transition: all var(--transition-fast);
+  transition: background-color var(--transition-fast), border-color var(--transition-fast), box-shadow var(--transition-fast), transform var(--transition-fast), opacity var(--transition-fast), color var(--transition-fast);
   color: var(--color-gray-300);
 }
 
@@ -1219,11 +1219,10 @@ label {
   }
 }
 
-/* General overflow prevention */
-* {
-  max-width: 100%;
-}
-
+/* General overflow prevention — scoped to media only.
+   (Previously `* { max-width: 100% }` applied to every element on every
+   page, which fights flexbox/grid sizing and is the main cause of
+   elements shrinking/misaligning inconsistently across pages.) */
 img, video, svg, canvas {
   max-width: 100%;
   height: auto;
diff --git a/src/styles/worker.css b/src/styles/worker.css
index 2977639..6aaa39d 100644
--- a/src/styles/worker.css
+++ b/src/styles/worker.css
@@ -14,8 +14,8 @@
 
 .worker-glass-panel {
   background: rgba(20, 25, 35, 0.6);
-  
-  -webkit-
+  backdrop-filter: blur(16px);
+  -webkit-backdrop-filter: blur(16px);
   border: 1px solid rgba(255, 255, 255, 0.08);
   border-radius: 20px;
   padding: 1.5rem;
